# Recommended interpreter for local and GitHub Actions builds: Python 3.11 x64.
# OR-Tools is pinned because it is a native-wheel dependency and PyInstaller
# desktop builds are more predictable with a fixed wheel version.
numpy>=1.26,<3
pandas>=2.1,<3
matplotlib>=3.8,<4
PySide6>=6.6,<7
ortools==9.15.6755
pyinstaller>=6.11,<7
pyinstaller-hooks-contrib>=2025.1
