# Recommended interpreter for local and GitHub Actions builds: Python 3.11 x64.
# OR-Tools is a native-wheel dependency, so avoiding Python 3.13 is safer for desktop builds.
numpy>=1.26,<3
pandas>=2.1,<3
matplotlib>=3.8,<4
PySide6>=6.6,<7
ortools>=9.12,<10
pyinstaller>=6.11,<7
pyinstaller-hooks-contrib>=2025.1
